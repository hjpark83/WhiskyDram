/**
 * 기능 켜고 끄기.
 *
 * 다시 볼 기능은 지우지 말고 여기서 꺼요. 코드는 그대로 남아서 한 줄만 바꾸면
 * 돌아와요 (지웠다가 되살리면 그 사이에 다른 코드가 바뀌어 안 맞아요).
 *
 * 꺼진 기능은 메뉴에서 빠지고, 주소로 직접 들어가도 404 예요.
 */
export const FEATURES = {
  /**
   * 팝업 스토어 (/popup, /admin/popups).
   *
   * "정보를 어디서 어떻게 받아올지" 를 정하고 켰어요. 답은 **가져와서 우리
   * 화면에 정리해두기** 예요 — 네이버 링크만 걸어두면 사용자가 검색 결과를
   * 다시 헤매야 하니까요.
   *
   * 받아오는 방법은 `src/lib/ai/web-research.ts` 의 검색 그라운딩이에요.
   * 네이버·캐치테이블 HTML 을 긁지 않아요 (약관 문제도 있고, 두 곳 다 로그인·
   * 봇 차단이 걸린 JS 페이지라 서버에서 긁으면 금방 막혀요).
   *
   * 대신 틀리면 헛걸음이 되는 정보(기간·주소·가격)라서 두 가지를 지켜요.
   *  - AI 초안은 `published: false` 로 저장되고 관리자가 확인한 뒤 공개돼요
   *  - 공개된 화면에도 **출처와 확인 날짜**를 같이 보여줘요
   */
  popup: true,
} as const;
